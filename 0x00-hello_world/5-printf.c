#include <stdio.h>
/**
*main - Print a text
*
*Return: by default return zero if no errors encountered
*/
int main(void)
{
	printf("with proper grammar, but the outcomeis a piece of art,\n");
	return (0);
}
